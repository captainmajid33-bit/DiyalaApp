import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.diyala.app',
  appName: 'DiyalaApp',
  webDir: 'npx cap add android'
};

export default config;
